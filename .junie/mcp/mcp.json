{
    "mcpServers": {
        "Honcho": {
            "command": "npx",
            "args": [
                "-y",
                "mcp-remote",
                "https://mcp.honcho.dev",
                "--header",
                "Authorization:${AUTH_HEADER}",
                "--header",
                "X-Honcho-User-Name:${USER_NAME}"
            ],
            "env": {
                "AUTH_HEADER": "${HONCHO_AUTH_HEADER}",
                "USER_NAME": "${HONCHO_USER_NAME}"
            }
        }
    }
}