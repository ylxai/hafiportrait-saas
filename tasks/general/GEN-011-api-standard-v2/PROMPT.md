# Task: GEN-011 - API Standard V2
**Size:** M
**Mission:** Use `safeParse` and `errorResponse` in all API routes.

## Steps
### Step 1: Standardize Zod
- [ ] Audit all API routes for `.parse()`
- [ ] Implement `safeParse` and `errorResponse(message, 400)`
- [ ] Ensure `body: unknown` is used
